{
  "name": "@minecraft.js/root",
  "private": true,
  "scripts": {
    "build": "bun run --filter './packages/*' build",
    "docs": "bun run --filter docs dev",
    "format": "bun --filter '*' format",
    "lint": "bun --filter '*' lint"
  },
  "workspaces": [
    "docs",
    "playground",
    "packages/*"
  ],
  "dependencies": {
    "@biomejs/biome": "1.9.4",
    "lefthook": "1.11.13"
  }
}
